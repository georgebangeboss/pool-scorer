<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.WinnerPage"></activity>
        <activity android:name=".gui.MatPot" />
        <activity android:name=".gui.Preview" />
        <activity android:name=".gui.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.CurrentPlayerScreen"
            android:launchMode="singleTop" />
        <activity android:name=".gui.InputScreen" />
        <activity android:name=".gui.MatKiss" />
    </application>

</manifest>